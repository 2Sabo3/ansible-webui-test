---

- hosts: all
  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
    - name: buri buri zai mon
      shell: cd Desktop && mkdir shinchan
